# This TOML file must contain the exact configuration keys as defined in app/config/types.py TypedDicts.
# Otherwise, the app will fail to load.
[app]
title = "Fastup"
version = "0.1.0"

[server]
host = "0.0.0.0"
port = 8000
debug = true

[database]
url = "sqlite+aiosqlite:///./db.sqlite3"
echo = false # set to True for debugging purposes
